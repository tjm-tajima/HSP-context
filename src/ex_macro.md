# ex_macro

- **種別**: マクロ
- **グループ**: 標準定義マクロ
- **バージョン**: 3.3
- **更新日**: 2011/05/10
- **作者**: onitama
- **URL**: http://hsp.tv/
- **対応環境**: Win / Cli / Let

## 命令一覧

| 命令 | 概要 |
|---|---|
| [_debug](#_debug) | デバッグモード時 |
| [__hsp30__](#__hsp30__) | ver3.0以降使用時 |
| [__file__](#__file__) | 使用時点で解析されているファイル名 |
| [__line__](#__line__) | 使用時点で解析されている行番号 |
| [__date__](#__date__) | 使用時点の日付 |
| [__time__](#__time__) | 使用時点の時刻 |
| [__hspver__](#__hspver__) | HSPバージョン番号 |

## 命令詳細

### _debug

デバッグモード時

**構文**
```
_debug
```

**パラメータ**
- なし

**説明**

デバッグモード時のみ定義されるマクロです。
\#ifdef、#ifndef命令などで判別して分岐させることが可能です。

**関連**: hspstat

**サンプル**
```hsp
#ifdef _debug
	mes "デバッグモードです。"
#else
	mes "デバッグモードではありません。"
#endif
	stop
```

---

### __hsp30__

ver3.0以降使用時

**構文**
```
__hsp30__
```

**パラメータ**
- なし

**説明**

HSP3.0以降で、「HSP拡張マクロを使用する」にチェックが入っている時に定義されます。

**サンプル**
```hsp
#ifdef __hsp30__
	mes {"HSP3.0以降で、「HSP拡張マクロを使用する」に
	チェックが入っています。"}
#else
	mes {"HSP3.0より古いか、「HSP拡張マクロを使用する」に
	チェックが入っていません。"}
#endif
	stop
```

---

### __file__

使用時点で解析されているファイル名

**構文**
```
__file__
```

**パラメータ**
- なし

**説明**

使用時点で解析されているファイル名に置き換えられます。
ファイルが保存されていない場合は"???"に置き換わります。

**関連**: __line__

**サンプル**
```hsp
	mes "使用時点で解析されているファイル名は" + __file__
	stop
```

---

### __line__

使用時点で解析されている行番号

**構文**
```
__line__
```

**パラメータ**
- なし

**説明**

使用時点で解析されている行番号に置き換わります。行番号は1から始まる整数値です。

**関連**: __file__

**サンプル**
```hsp
	mes "使用時点で解析されている行番号は" + __line__
	stop
```

---

### __date__

使用時点の日付

**構文**
```
__date__
```

**パラメータ**
- なし

**説明**

使用時点（コンパイルした時点）の日付に置き換わります。日付はYYYY/MM/DD形式で表されます。

「HSP拡張マクロを使用する」にチェックが入っていないと使用できません。

**関連**: __time__

**サンプル**
```hsp
	mes "使用時点の日付は" + __date__
	stop
```

---

### __time__

使用時点の時刻

**構文**
```
__time__
```

**パラメータ**
- なし

**説明**

使用時点（コンパイルした時点）の時刻に置き換わります。時刻はHH:MM:SS形式で表されます。

「HSP拡張マクロを使用する」にチェックが入っていないと使用できません。

**関連**: __date__

**サンプル**
```hsp
	mes "使用時点の時刻は" + __time__
	stop
```

---

### __hspver__

HSPバージョン番号

**構文**
```
__hspver__
```

**パラメータ**
- なし

**説明**

HSPのバージョン番号に置き換わります。上位8bit、下位8bitがそれぞれメジャーバージョン・マイナーバージョンを示します。 

「HSP拡張マクロを使用する」にチェックが入っていないと使用できません。

**関連**: hspver

**サンプル**
```hsp
	mes "HSPのバージョン番号は" + strf("%4x", __hspver__)
	stop
```

---
