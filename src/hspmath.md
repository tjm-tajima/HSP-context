# hspmath

- **種別**: ユーザー定義マクロ
- **グループ**: 数学関数
- **バージョン**: 3.6
- **更新日**: 2011/05/10
- **作者**: HSPWiki
- **対応環境**: Win / Mac / Cli / Let
- **備考**: hspmath.asをインクルードすること。

## 命令一覧

| 命令 | 概要 |
|---|---|
| [M_LOG2E](#m_log2e) | 2を底とするネイピア数の対数 |
| [M_LOG10E](#m_log10e) | 10を底とするネイピア数の対数 |
| [M_LN2](#m_ln2) | ネイピア数を底とした2の対数 |
| [M_LN10](#m_ln10) | ネイピア数を底とした10の対数 |
| [M_E](#m_e) | ネイピア数 |
| [M_SQRTPI](#m_sqrtpi) | 円周率の平方根 |
| [M_SQRT2](#m_sqrt2) | 2の平方根 |
| [M_SQRT3](#m_sqrt3) | 3の累乗根 |
| [DBL_DIG](#dbl_dig) | 10進数で有効な桁数 |
| [DBL_EPSILON](#dbl_epsilon) | 1.0とその次に大きい値との差 |
| [DBL_MANT_DIG](#dbl_mant_dig) | 仮数部のbit数 |
| [DBL_MAX](#dbl_max) | 実数の最大値 |
| [DBL_MAX_10_EXP](#dbl_max_10_exp) | 10進数での指数部の最大値 |
| [DBL_MAX_EXP](#dbl_max_exp) | 2進数での指数部の最大値 |
| [DBL_MIN](#dbl_min) | 0を超える最小の値 |
| [DBL_MIN_10_EXP](#dbl_min_10_exp) | 10進数での指数部の最小値 |
| [DBL_MIN_EXP](#dbl_min_exp) | 2進数での指数部の最小値 |
| [INT_DIGIT](#int_digit) | 2進数で有効な桁数 |
| [INT_DIGIT10](#int_digit10) | 10進数で有効な桁数 |
| [INT_MAX](#int_max) | 最大値 |
| [INT_MIN](#int_min) | 最小値 |
| [pow](#pow) | 累乗（べき乗）を求める |
| [log10](#log10) | 10を底とした対数（常用対数） |
| [log2](#log2) | 2を底とした対数 |
| [asin](#asin) | サインの逆関数（アークサイン） |
| [acos](#acos) | コサインの逆関数（アークコサイン） |
| [sinh](#sinh) | 双曲線正弦関数（ハイパボリックサイン） |
| [cosh](#cosh) | 双曲線余弦関数（ハイパボリックコサイン） |
| [tanh](#tanh) | 双曲線正接関数（ハイパボリックタンジェント） |
| [asinh](#asinh) | 双曲線正弦関数の逆関数（アークハイパボリックサイン） |
| [acosh](#acosh) | 双曲線余弦関数の逆関数（アークハイパボリックコサイン） |
| [atanh](#atanh) | 双曲線正接関数の逆関数（アークハイパボリックタンジェント） |
| [isfinite](#isfinite) | 有限／無限･非数の判定 |
| [isnan](#isnan) | 非数の判定 |
| [round](#round) | 四捨五入 |
| [sgn](#sgn) | 符号 |
| [intf](#intf) | 0の方向へ丸め |
| [floor](#floor) | 負の方向へ丸め |
| [ceil](#ceil) | 正の方向へ丸め |
| [fmod](#fmod) | モジュロ |
| [distance2](#distance2) | 2点の距離を求める |

## 命令詳細

### M_LOG2E

2を底とするネイピア数の対数

- **グループ**: 数学定数

**構文**
```
M_LOG2E
```

**パラメータ**
- なし

**説明**

2を底とするネイピア数の対数を表す定数です。
2を底とするネイピア数の対数とは、log2(m_e)のことを指します。

**関連**: M_LOG10E, M_E, log2

**サンプル**
```hsp
#include "hspmath.as"
	a = 10.0
	mes "2を底とするaの対数は" + log2(a) + "です。"
	mes "これはlogf(a) * m_log2eとしても求められます：" + (logf(a) * m_log2e) + "\n"

	mes "このようにm_log2eを「eを底とする対数」にかけることで、"
	mes "「2を底とする対数」を得ることができます。"
	stop
```

---

### M_LOG10E

10を底とするネイピア数の対数

- **グループ**: 数学定数

**構文**
```
M_LOG10E
```

**パラメータ**
- なし

**説明**

10を底とするネイピア数の対数を表す定数です。
10を底とするネイピア数の対数とは、log10(m_e)のことを指します。

**関連**: M_LOG2E, log10, M_E

**サンプル**
```hsp
#include "hspmath.as"
	a = m_e
	mes "10を底とするaの対数は" + log10(a) + "です。"
	mes "これはlogf(a) * m_log10eとしても求められます：" + (logf(a) * m_log10e) + "\n"

	mes "このようにm_log10eを「eを底とする対数」にかけることで、"
	mes "「10を底とする対数」を得ることができます。"
	stop
```

---

### M_LN2

ネイピア数を底とした2の対数

- **グループ**: 数学定数

**構文**
```
M_LN2
```

**パラメータ**
- なし

**説明**

ネイピア数を底とした2の対数を表す定数です。
ネイピア数を底とした2の対数とは、logf(2)のことを指します。

**関連**: M_LN10

---

### M_LN10

ネイピア数を底とした10の対数

- **グループ**: 数学定数

**構文**
```
M_LN10
```

**パラメータ**
- なし

**説明**

ネイピア数を底とした10の対数を表す定数です。
ネイピア数を底とした10の対数とは、logf(10)のことを指します。

**関連**: M_LN2

---

### M_E

ネイピア数

- **グループ**: 数学定数

**構文**
```
M_E
```

**パラメータ**
- なし

**説明**

ネイピア数（自然対数の底）eを表す定数です。

**関連**: M_PI, M_LOG2E, M_LOG10E

---

### M_SQRTPI

円周率の平方根

- **グループ**: 数学定数

**構文**
```
M_SQRTPI
```

**パラメータ**
- なし

**説明**

円周率の平方根を表す定数です。

**関連**: sqrt, M_PI, M_SQRT2, M_SQRT3

---

### M_SQRT2

2の平方根

- **グループ**: 数学定数

**構文**
```
M_SQRT2
```

**パラメータ**
- なし

**説明**

2の平方根を表す定数です。

**関連**: sqrt, M_SQRTPI, M_SQRT3

---

### M_SQRT3

3の累乗根

- **グループ**: 数学定数

**構文**
```
M_SQRT3
```

**パラメータ**
- なし

**説明**

3の累乗根を表す定数です。

**関連**: sqrt, M_SQRTPI, M_SQRT2

---

### DBL_DIG

10進数で有効な桁数

- **グループ**: doubleの限度

**構文**
```
DBL_DIG
```

**パラメータ**
- なし

**説明**

10進数で有効な桁数を表す定数です。

---

### DBL_EPSILON

1.0とその次に大きい値との差

- **グループ**: doubleの限度

**構文**
```
DBL_EPSILON
```

**パラメータ**
- なし

**説明**

1.0とその次に大きい値との差を表す定数です。

---

### DBL_MANT_DIG

仮数部のbit数

- **グループ**: doubleの限度

**構文**
```
DBL_MANT_DIG
```

**パラメータ**
- なし

**説明**

実数型の数値を表す際に用いる仮数部のbit数を表す定数です。

---

### DBL_MAX

実数の最大値

- **グループ**: doubleの限度

**構文**
```
DBL_MAX
```

**パラメータ**
- なし

**説明**

実数で表現できる最大値を表す定数です。

**関連**: INT_MAX

---

### DBL_MAX_10_EXP

10進数での指数部の最大値

- **グループ**: doubleの限度

**構文**
```
DBL_MAX_10_EXP
```

**パラメータ**
- なし

**説明**

指数部の最大値を表す定数です。

**関連**: DBL_MIN_10_EXP, DBL_MAX_EXP

---

### DBL_MAX_EXP

2進数での指数部の最大値

- **グループ**: doubleの限度

**構文**
```
DBL_MAX_EXP
```

**パラメータ**
- なし

**説明**

指数部の最大値を表す定数です。

**関連**: DBL_MAX_10_EXP, INT_MAX

---

### DBL_MIN

0を超える最小の値

- **グループ**: doubleの限度

**構文**
```
DBL_MIN
```

**パラメータ**
- なし

**説明**

0を超える最小の値を表す定数です。

---

### DBL_MIN_10_EXP

10進数での指数部の最小値

- **グループ**: doubleの限度

**構文**
```
DBL_MIN_10_EXP
```

**パラメータ**
- なし

**説明**

指数部の最小値を表す定数です。

**関連**: DBL_MAX_10_EXP, DBL_MIN_EXP

---

### DBL_MIN_EXP

2進数での指数部の最小値

- **グループ**: doubleの限度

**構文**
```
DBL_MIN_EXP
```

**パラメータ**
- なし

**説明**

指数部の最小値を表す定数です。

**関連**: DBL_MIN_10_EXP

---

### INT_DIGIT

2進数で有効な桁数

- **グループ**: intの限度

**構文**
```
INT_DIGIT
```

**パラメータ**
- なし

**説明**

2進数で有効な桁数を表す定数です。

**関連**: INT_DIGIT10

---

### INT_DIGIT10

10進数で有効な桁数

- **グループ**: intの限度

**構文**
```
INT_DIGIT10
```

**パラメータ**
- なし

**説明**

10進数で有効な桁数を表す定数です。

**関連**: INT_DIGIT

---

### INT_MAX

最大値

- **グループ**: intの限度

**構文**
```
INT_MAX
```

**パラメータ**
- なし

**説明**

整数で表現できる最大値を表す定数です。

**関連**: DBL_MAX, INT_MIN

---

### INT_MIN

最小値

- **グループ**: intの限度

**構文**
```
INT_MIN
```

**パラメータ**
- なし

**説明**

整数で表現できる最小の値を表す定数です。

**関連**: INT_MAX

---

### pow

累乗（べき乗）を求める

**構文**
```
pow(p1, p2)
```

**パラメータ**
- `p1`: 底（0以上）
- `p2`: 指数

**説明**

p1をp2乗した値を求めます。結果は実数で与えられます。

p1は必ず正でなければなりません。負の場合はエラーにはなりませんが、非数（-1.#IND00）が返ります。

p2は正負どちらでも構いません。また、実数を指定することも可能です。

**関連**: powf, logf

**サンプル**
```hsp
#include "hspmath.as"
	repeat 5, -2
		mes "10の" + cnt + "乗は" + pow(10, cnt) + "です。"
	loop
	stop
```

---

### log10

10を底とした対数（常用対数）

**構文**
```
log10(p1)
```

**パラメータ**
- `p1`: 真数

**説明**

10を底とするp1の対数を求めます。結果は実数で与えられます。

**関連**: M_LOG10E, logf, log2

---

### log2

2を底とした対数

**構文**
```
log2(p1)
```

**パラメータ**
- `p1`: 真数

**説明**

2を底とするp1の対数を求めます。結果は実数で与えられます。

**関連**: M_LOG2E, logf, log10

---

### asin

サインの逆関数（アークサイン）

**構文**
```
asin(p1)
```

**パラメータ**
- `p1`: 角度値（ラジアン）

**説明**

p1のアークサイン（逆正弦）値を返します。
結果は実数型かつラジアン、-M_PI/2～M_PI/2（度数法で-90°～90°）の範囲で与えられます。

**関連**: acos, atan

---

### acos

コサインの逆関数（アークコサイン）

**構文**
```
acos(p1)
```

**パラメータ**
- `p1`: 角度値（ラジアン）

**説明**

p1のアークコサイン（逆余弦）値を返します。
結果は実数型かつラジアン、0～M_PI（度数法で0°～180°）の範囲で与えられます。

**関連**: asin, atan

---

### sinh

双曲線正弦関数（ハイパボリックサイン）

**構文**
```
sinh(p1)
```

**パラメータ**
- `p1`: 角度値（ラジアン）

**説明**

p1のハイパボリックサイン（双曲線正弦）値を返します。
結果は実数で与えられます。

**関連**: cosh, tanh, asinh

---

### cosh

双曲線余弦関数（ハイパボリックコサイン）

**構文**
```
cosh(p1)
```

**パラメータ**
- `p1`: 角度値（ラジアン）

**説明**

p1のハイパボリックコサイン（双曲線余弦）値を返します。
結果は実数で与えられます。

**関連**: sinh, tanh, acosh

---

### tanh

双曲線正接関数（ハイパボリックタンジェント）

**構文**
```
tanh(p1)
```

**パラメータ**
- `p1`: 角度値（ラジアン）

**説明**

p1のハイパボリックタンジェント（双曲線正接）値を返します。
結果は実数で与えられます。

**関連**: sinh, cosh, atanh

---

### asinh

双曲線正弦関数の逆関数（アークハイパボリックサイン）

**構文**
```
asinh(p1)
```

**パラメータ**
- `p1`: 角度値（ラジアン）

**説明**

p1のアークハイパボリックサイン（逆双曲線正弦）値を返します。
結果は実数で与えられます。

**関連**: acosh, atanh, sinh

---

### acosh

双曲線余弦関数の逆関数（アークハイパボリックコサイン）

**構文**
```
acosh(p1)
```

**パラメータ**
- `p1`: 角度値（ラジアン）

**説明**

p1のアークハイパボリックコサイン（逆双曲線余弦）値を返します。
結果は実数で与えられます。

**関連**: asinh, atanh, cosh

---

### atanh

双曲線正接関数の逆関数（アークハイパボリックタンジェント）

**構文**
```
atanh(p1)
```

**パラメータ**
- `p1`: 角度値（ラジアン）

**説明**

p1のアークハイパボリックタンジェント（逆双曲線正接）値を返します。
結果は実数で与えられます。

**関連**: asinh, acosh, tanh

---

### isfinite

有限／無限･非数の判定

**構文**
```
isfinite(p1)
```

**パラメータ**
- `p1`: 判定する値

**説明**

p1が有限の値ならば1を、p1が無限または非数の時は0を返します。

非数とは数値以外の型（文字列やラベル、モジュール型など）を指すものではありません。変数の型を調べる時はvartype関数を使用してください。

**関連**: isnan, vartype

**サンプル**
```hsp
#include "hspmath.as"
	mes isfinite(10)            // 有限の値
	mes isfinite(sqrt(-1))      // 非数
	mes isfinite(expf(100))     // 有限の値
	mes isfinite(expf(1000))    // 無限大
	stop
```

---

### isnan

非数の判定

**構文**
```
isnan(p1)
```

**パラメータ**
- `p1`: 判定する値

**説明**

p1が非数の時は1を、それ以外の時は0を返します。

非数とは数値以外の型（文字列やラベル、モジュール型など）を指すものではありません。変数の型を調べる時はvartype関数を使用してください。

**関連**: isfinite, vartype

**サンプル**
```hsp
#include "hspmath.as"
	mes isnan(10)               // 有限の値
	mes isnan(sqrt(-1))         // 非数
	mes isnan(expf(100))        // 有限の値
	mes isnan(expf(1000))       // 無限大
	stop
```

---

### round

四捨五入

**構文**
```
round(p1)
```

**パラメータ**
- `p1`: 四捨五入する値（実数）

**説明**

p1の小数点以下を四捨五入し、その結果を実数で返します。

**関連**: intf, floor, ceil

**サンプル**
```hsp
#include "hspmath.as"
	repeat 10
		tmp = 1.0 + 0.1 * cnt
		mes strf("%0.1fを四捨五入すると", tmp) + strf("%0.1fになります。", round(tmp))
	loop
	stop
```

---

### sgn

符号

**構文**
```
sgn(p1)
```

**パラメータ**
- `p1`: 符号を判定する値（数値）

**説明**

数値の符号を判定し、正ならば1を・負ならば-1を・ゼロならば0を返します。

**サンプル**
```hsp
#include "hspmath.as"
	tmp = 10
	sign = sgn(tmp)
	if sign == 1 {
		mes str(tmp) + "は正です。"
	} else : if sign == -1 {
		mes str(tmp) + "は負です。"
	} else {
		mes str(tmp) + "はゼロです。"
	}
	stop
```

---

### intf

0の方向へ丸め

**構文**
```
intf(p1)
```

**パラメータ**
- `p1`: 丸める数値

**説明**

p1を0の方向に丸めた数値を返します。結果は実数で返されます。

ここで「p1を0の方向に丸める」とは、「p1の小数点以下を切り捨てる」ことを意味します。

**関連**: round, floor, ceil

**サンプル**
```hsp
#include "hspmath.as"
	tmp = -2.5
	while(tmp <= 2.5)
		mes strf("%4.1f", tmp) + "を0の方向へ丸めると、" + strf("%4.1f", intf(tmp)) + "になります。"
		tmp += 0.5
	wend
	stop
```

---

### floor

負の方向へ丸め

**構文**
```
floor(p1)
```

**パラメータ**
- `p1`: 丸める数値

**説明**

p1を負の方向に丸めた数値を返します。結果は実数で返されます。

ここで「p1を負の方向に丸める」とは、
html{
<ul><li>p1が正ならば小数点以下を切り捨てる</li><li>p1が負ならば小数点以下を切り上げる</ul>
}html
ことを意味します。

**関連**: round, intf, ceil

**サンプル**
```hsp
#include "hspmath.as"
	tmp = -2.5
	while(tmp <= 2.5)
		mes strf("%4.1f", tmp) + "を負の方向へ丸めると、" + strf("%4.1f", floor(tmp)) + "になります。"
		tmp += 0.5
	wend
	stop
```

---

### ceil

正の方向へ丸め

**構文**
```
ceil(p1)
```

**パラメータ**
- `p1`: 丸める数値

**説明**

p1を正の方向に丸めた数値を返します。結果は実数で返されます。

ここで「p1を正の方向に丸める」とは、
html{
<ul><li>p1が正ならば小数点以下を切り上げる</li><li>p1が負ならば小数点以下を切り捨てる</ul>
}html
ことを意味します。

**関連**: round, intf, floor

**サンプル**
```hsp
#include "hspmath.as"
	tmp = -2.5
	while(tmp <= 2.5)
		mes strf("%4.1f", tmp) + "を正の方向へ丸めると、" + strf("%4.1f", ceil(tmp)) + "になります。"
		tmp += 0.5
	wend
	stop
```

---

### fmod

モジュロ

**構文**
```
fmod(p1, p2)
```

**パラメータ**
- `p1`: 割られる数
- `p2`: 割る数

**説明**

p1をp2で割ったときの余りを実数で返します。
\記号による演算とは異なり、実数に対しても有効です。

**サンプル**
```hsp
#include "hspmath.as"
	mes 5 \ 2
	mes fmod(5, 2)      // 戻り値は実数となる
	mes fmod(3.5, 1.2)  // 実数を実数で割ったときの余りも求められる
	stop
```

---

### distance2

2点の距離を求める

**構文**
```
distance2(p1, p2)
```

**パラメータ**
- `p1, p2`: 点の座標を代入した数値型配列変数

**説明**

2点A，B間の距離を求めます。

p1とp2には点の座標を直交座標系で代入しておきます。
html{
<ul><li>p1(0)に点AのX座標</li><li>p1(1)に点AのY座標</li><li>p2(0)に点BのX座標</li><li>p2(1)に点BのY座標</li></ul>
}html
を代入した状態で呼び出してください。
結果は実数で返されます。

**サンプル**
```hsp
#include "hspmath.as"
	point_a = 100, 50       // 点A
	point_b = 500, 400      // 点B
	mes "2点間の距離は" + distance2(point_a, point_b) + "です。"
	line point_a(0), point_a(1), point_b(0), point_b(1)
	color 255  : pset point_a(0), point_a(1)    // 点A
	color ,255 : pset point_b(0), point_b(1)    // 点B
	stop
```

---
